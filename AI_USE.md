# AI Use Log

Tool used throughout this project: **Claude** (Anthropic)

| # | What I asked it to do | What I kept / changed / rejected, and why |
|---|---|---|
| 1 | Give step-by-step instructions for setting up the project in Codespaces (installing `uv`, running `uv init`, adding dependencies) | Followed the steps as given. When `source $HOME/.local/bin/env` failed because that file didn't exist, I asked for help and used the suggested fix (`export PATH="$HOME/.local/bin:$PATH"`) instead — Claude adjusted its instructions after seeing my actual terminal output rather than assuming the standard path was correct. |
| 2 | Review my GitHub "Create repository" settings screenshot before I created the repo | Kept the suggestion to turn on "Add README" and use the Python `.gitignore` template. Used `amazon-sales-dashboard` as the repo name. |
| 3 | Write the data-cleaning code for the messy CSV fields (₹ prices, % discount, a non-numeric rating placeholder, comma-separated rating counts) | Kept the core approach (strip characters, cast to numeric) after asking Claude to explain the tradeoff between dropping bad rows vs. converting them to blank/NaN. I chose the "convert to NaN, keep the row" approach based on its explanation, since it preserves the rest of that row's valid data. |
| 4 | Build the full `app.py` Streamlit dashboard (title, metrics, 4 charts, takeaways) using my actual uploaded CSV | Kept the generated file after Claude tested it against my real data and confirmed the row counts and averages matched (1,465 rows, 1 rating coerced to NaN). Reviewed the code myself afterward and had Claude walk through what each part does (the `@st.cache_data` decorator, the `.str.replace()` chains, `pd.to_numeric(errors="coerce")`, and `.groupby()`) so I could explain it if asked in class. |
| 5 | Fix a `use_container_width` deprecation warning in the terminal output | Kept the fix (`width='stretch'`) after confirming it was just a future-compatibility warning, not a bug — the dashboard worked correctly either way. |
| 6 | Draft `README.md` with setup instructions and cleaning justifications | Kept the structure and cleaning explanations largely as generated, since they matched decisions I had already made. Did not add my name to the README yet — noted as an optional follow-up. |
| 7 | Build a first draft of the 10-slide PowerPoint using pptxgenjs, based on real numbers computed from my dataset | Did not use the generated `.pptx` file directly. Instead asked Claude to explain the code, then had it give me slide-by-slide text content (titles, body copy, takeaway sentences) so I could build the deck myself in Google Slides, using my own dashboard screenshots for the chart slides instead of recreated charts. |
| 8 | Explain what the `discount_percentage` cleaning code does, and why it results in `64.0` instead of `0.64` | Used this explanation to understand the code well enough to defend it, rather than changing anything — confirmed `64.0` was correct given how the value is displayed later in the dashboard (`f"{...:.0f}%"`). |
| 9 | Generate this AI-use log from the conversation history | Reviewed and will edit as needed before submitting — this log itself was drafted by Claude, then checked against what actually happened. |
